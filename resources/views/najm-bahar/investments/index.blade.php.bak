@extends('layouts.unified')

@section('title', 'سرمایه‌گذاری - نجم بهار')

@section('content')
<div class="container mx-auto px-4 py-8" dir="rtl">
    <!-- هدر صفحه -->
    <div class="mb-8">
        <h1 class="text-3xl font-bold text-gray-900">فرصت‌های سرمایه‌گذاری</h1>
        <p class="text-gray-600 mt-2">پروژه‌های تایید شده برای جذب سرمایه</p>
    </div>

    <!-- فیلترها -->
    <div class="bg-white rounded-lg shadow p-6 mb-8">
        <form method="GET" action="{{ route('najm-bahar.investments.index') }}" class="grid grid-cols-1 md:grid-cols-4 gap-4">
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">دسته‌بندی</label>
                <select name="category" class="w-full rounded-lg border-gray-300 shadow-sm focus:border-green-500 focus:ring-green-500">
                    <option value="">همه دسته‌ها</option>
                    @foreach($categories as $category)
                        <option value="{{ $category->id }}" {{ request('category') == $category->id ? 'selected' : '' }}>
                            {{ $category->name }}
                        </option>
                    @endforeach
                </select>
            </div>

            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">نوع پروژه</label>
                <select name="type" class="w-full rounded-lg border-gray-300 shadow-sm focus:border-green-500 focus:ring-green-500">
                    <option value="">همه انواع</option>
                    <option value="public" {{ request('type') == 'public' ? 'selected' : '' }}>عمومی</option>
                    <option value="private" {{ request('type') == 'private' ? 'selected' : '' }}>خصوصی</option>
                </select>
            </div>

            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">مرتب‌سازی</label>
                <select name="sort_by" class="w-full rounded-lg border-gray-300 shadow-sm focus:border-green-500 focus:ring-green-500">
                    <option value="created_at" {{ request('sort_by') == 'created_at' ? 'selected' : '' }}>جدیدترین</option>
                    <option value="profit_percentage" {{ request('sort_by') == 'profit_percentage' ? 'selected' : '' }}>بیشترین سود</option>
                    <option value="required_capital" {{ request('sort_by') == 'required_capital' ? 'selected' : '' }}>مبلغ سرمایه</option>
                </select>
            </div>

            <div class="flex items-end">
                <button type="submit" class="w-full px-6 py-2 bg-green-600 hover:bg-green-700 text-white rounded-lg shadow transition-colors">
                    <i class="fas fa-filter ml-2"></i>
                    اعمال فیلتر
                </button>
            </div>
        </form>
    </div>

    <!-- لیست پروژه‌ها -->
    @if($projects->isEmpty())
        <div class="bg-white rounded-lg shadow text-center py-16">
            <i class="fas fa-inbox text-gray-300 text-6xl mb-4"></i>
            <p class="text-gray-500 text-lg">در حال حاضر پروژه‌ای برای سرمایه‌گذاری موجود نیست</p>
        </div>
    @else
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            @foreach($projects as $project)
                <div class="bg-white rounded-lg shadow hover:shadow-xl transition-shadow overflow-hidden">
                    <!-- هدر کارت -->
                    <div class="bg-gradient-to-r from-green-600 to-teal-600 text-white p-6">
                        <h3 class="text-xl font-bold mb-2">{{ $project->title }}</h3>
                        <p class="text-green-100 text-sm">{{ $project->category_path }}</p>
                    </div>

                    <!-- محتوای کارت -->
                    <div class="p-6">
                        <!-- خلاصه -->
                        <p class="text-gray-600 text-sm mb-4 line-clamp-3">{{ $project->summary }}</p>

                        <!-- اطلاعات کلیدی -->
                        <div class="space-y-3 mb-6">
                            <div class="flex justify-between items-center">
                                <span class="text-gray-600 text-sm">سرمایه مورد نیاز:</span>
                                <span class="font-bold text-gray-900">{{ number_format($project->required_capital) }} گل</span>
                            </div>
                            <div class="flex justify-between items-center">
                                <span class="text-gray-600 text-sm">درصد سود:</span>
                                <span class="font-bold text-green-600">{{ $project->profit_percentage }}%</span>
                            </div>
                            @if($project->investment_duration_months)
                                <div class="flex justify-between items-center">
                                    <span class="text-gray-600 text-sm">مدت سرمایه‌گذاری:</span>
                                    <span class="font-semibold text-gray-900">{{ $project->investment_duration_months }} ماه</span>
                                </div>
                            @endif
                        </div>

                        <!-- پیشرفت سرمایه‌گذاری -->
                        @php
                            $progress = $project->investment_progress ?? 0;
                            $raised = $project->total_invested ?? 0;
                        @endphp
                        <div class="mb-4">
                            <div class="flex justify-between text-sm mb-2">
                                <span class="text-gray-600">جمع‌آوری شده: {{ number_format($raised) }} گل</span>
                                <span class="font-semibold text-gray-900">{{ number_format($progress, 1) }}%</span>
                            </div>
                            <div class="w-full bg-gray-200 rounded-full h-2">
                                <div class="bg-green-600 h-2 rounded-full transition-all" style="width: {{ min(100, $progress) }}%"></div>
                            </div>
                        </div>

                        <!-- تعداد سرمایه‌گذار -->
                        <div class="flex items-center text-sm text-gray-600 mb-6">
                            <i class="fas fa-users ml-2"></i>
                            <span>{{ $project->investors_count ?? 0 }} سرمایه‌گذار</span>
                        </div>

                        <!-- دکمه سرمایه‌گذاری -->
                        <a href="{{ route('najm-bahar.investments.show', $project) }}" class="block w-full text-center px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded-lg shadow transition-colors">
                            <i class="fas fa-hand-holding-usd ml-2"></i>
                            مشاهده جزئیات و سرمایه‌گذاری
                        </a>
                    </div>
                </div>
            @endforeach
        </div>
    @endif
</div>
@endsection
