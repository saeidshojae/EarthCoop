@extends('layouts.unified')

@section('title', 'جزئیات سرمایه‌گذاری - نجم بهار')

@section('content')
<div class="container mx-auto px-4 py-8" dir="rtl">
    <div class="mb-8">
        <h1 class="text-3xl font-bold text-gray-900">{{ $project->title }}</h1>
        <p class="text-gray-600 mt-2">{{ $project->category_path }}</p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <div class="lg:col-span-2 space-y-6">
            <div class="bg-white rounded-lg shadow p-6">
                <h2 class="text-lg font-bold text-gray-900 mb-3">خلاصه طرح</h2>
                <p class="text-gray-700 leading-relaxed">{{ $project->summary }}</p>
            </div>

            <div class="bg-white rounded-lg shadow p-6">
                <h2 class="text-lg font-bold text-gray-900 mb-3">توضیحات کامل</h2>
                <p class="text-gray-700 leading-relaxed whitespace-pre-line">{{ $project->description ?? '—' }}</p>
            </div>
        </div>

        <div class="space-y-6">
            <div class="bg-white rounded-lg shadow p-6">
                <h2 class="text-lg font-bold text-gray-900 mb-4">اطلاعات سرمایه‌گذاری</h2>
                <div class="space-y-3 text-sm">
                    <div class="flex justify-between">
                        <span class="text-gray-600">سرمایه مورد نیاز:</span>
                        <span class="font-semibold">{{ number_format($project->required_capital) }} گل</span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-gray-600">جمع‌آوری شده:</span>
                        <span class="font-semibold">{{ number_format($stats['total_invested'] ?? 0) }} گل</span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-gray-600">باقی‌مانده:</span>
                        <span class="font-semibold">{{ number_format($stats['remaining'] ?? 0) }} گل</span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-gray-600">درصد سود:</span>
                        <span class="font-semibold text-green-600">{{ $project->profit_percentage }}%</span>
                    </div>
                    <div class="flex justify-between">
                        <span class="text-gray-600">مدت:</span>
                        <span class="font-semibold">{{ $project->investment_duration_months ?? '—' }} ماه</span>
                    </div>
                </div>
            </div>

            <div class="bg-white rounded-lg shadow p-6">
                <h2 class="text-lg font-bold text-gray-900 mb-4">ثبت سرمایه‌گذاری</h2>
                <form method="POST" action="{{ route('najm-bahar.investments.store', $project) }}">
                    @csrf
                    <div class="mb-4">
                        <label class="block text-sm font-medium text-gray-700 mb-2">مبلغ سرمایه‌گذاری (گل)</label>
                        <input type="number" name="amount" class="w-full rounded-lg border-gray-300" min="1" required>
                        @error('amount') <span class="text-red-500 text-xs">{{ $message }}</span> @enderror
                    </div>
                    <div class="mb-4">
                        <label class="block text-sm font-medium text-gray-700 mb-2">توضیحات (اختیاری)</label>
                        <textarea name="notes" rows="3" class="w-full rounded-lg border-gray-300"></textarea>
                        @error('notes') <span class="text-red-500 text-xs">{{ $message }}</span> @enderror
                    </div>
                    <button type="submit" class="w-full px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded-lg">
                        ثبت درخواست سرمایه‌گذاری
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
@endsection
