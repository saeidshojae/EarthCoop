@extends('layouts.unified')

@section('title', 'پروژه‌های من - نجم بهار')

@section('content')
<div class="container mx-auto px-4 py-8" dir="rtl">
    <!-- هدر صفحه -->
    <div class="flex justify-between items-center mb-8">
        <div>
            <h1 class="text-3xl font-bold text-gray-900">پروژه‌های من</h1>
            <p class="text-gray-600 mt-2">مدیریت و پیگیری پروژه‌های سرمایه‌گذاری</p>
        </div>
        <a href="{{ route('najm-bahar.projects.create') }}" class="px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded-lg shadow transition-colors">
            <i class="fas fa-plus ml-2"></i>
            ایجاد پروژه جدید
        </a>
    </div>

    <!-- آمار کلی -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
        <div class="bg-white rounded-lg shadow p-6">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-gray-600 text-sm">کل پروژه‌ها</p>
                    <p class="text-2xl font-bold text-gray-900 mt-1">{{ $stats['total'] ?? 0 }}</p>
                </div>
                <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center">
                    <i class="fas fa-folder text-blue-600 text-xl"></i>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-lg shadow p-6">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-gray-600 text-sm">تایید شده</p>
                    <p class="text-2xl font-bold text-green-600 mt-1">{{ $stats['approved'] ?? 0 }}</p>
                </div>
                <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center">
                    <i class="fas fa-check-circle text-green-600 text-xl"></i>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-lg shadow p-6">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-gray-600 text-sm">در انتظار بررسی</p>
                    <p class="text-2xl font-bold text-amber-600 mt-1">{{ ($stats['pending'] ?? 0) + ($stats['under_review'] ?? 0) }}</p>
                </div>
                <div class="w-12 h-12 bg-amber-100 rounded-lg flex items-center justify-center">
                    <i class="fas fa-clock text-amber-600 text-xl"></i>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-lg shadow p-6">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-gray-600 text-sm">پیش‌نویس</p>
                    <p class="text-2xl font-bold text-gray-600 mt-1">{{ $stats['draft'] ?? 0 }}</p>
                </div>
                <div class="w-12 h-12 bg-gray-100 rounded-lg flex items-center justify-center">
                    <i class="fas fa-file-alt text-gray-600 text-xl"></i>
                </div>
            </div>
        </div>
    </div>

    <!-- لیست پروژه‌ها -->
    <div class="bg-white rounded-lg shadow overflow-hidden">
        @if($projects->isEmpty())
            <div class="text-center py-16">
                <i class="fas fa-folder-open text-gray-300 text-6xl mb-4"></i>
                <p class="text-gray-500 text-lg">هنوز پروژه‌ای ایجاد نکرده‌اید</p>
                <a href="{{ route('najm-bahar.projects.create') }}" class="inline-block mt-4 px-6 py-2 bg-green-600 hover:bg-green-700 text-white rounded-lg transition-colors">
                    ایجاد اولین پروژه
                </a>
            </div>
        @else
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">عنوان</th>
                        <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">دسته‌بندی</th>
                        <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">سرمایه مورد نیاز</th>
                        <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">درصد سود</th>
                        <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">وضعیت</th>
                        <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">تاریخ ایجاد</th>
                        <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">عملیات</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                    @foreach($projects as $project)
                        <tr class="hover:bg-gray-50">
                            <td class="px-6 py-4">
                                <div class="text-sm font-medium text-gray-900">{{ $project->title }}</div>
                            </td>
                            <td class="px-6 py-4">
                                <div class="text-sm text-gray-600">{{ $project->category_path }}</div>
                            </td>
                            <td class="px-6 py-4">
                                <div class="text-sm text-gray-900 font-semibold">{{ number_format($project->required_capital) }} گل</div>
                            </td>
                            <td class="px-6 py-4">
                                <div class="text-sm text-green-600 font-semibold">{{ $project->profit_percentage }}%</div>
                            </td>
                            <td class="px-6 py-4">
                                @php
                                    $statusColors = [
                                        'draft' => 'gray',
                                        'pending' => 'amber',
                                        'under_review' => 'blue',
                                        'approved' => 'green',
                                        'rejected' => 'red',
                                        'archived' => 'slate',
                                    ];
                                    $statusLabels = [
                                        'draft' => 'پیش‌نویس',
                                        'pending' => 'در انتظار',
                                        'under_review' => 'در حال بررسی',
                                        'approved' => 'تایید شده',
                                        'rejected' => 'رد شده',
                                        'archived' => 'بایگانی',
                                    ];
                                    $color = $statusColors[$project->status] ?? 'gray';
                                    $label = $statusLabels[$project->status] ?? $project->status;
                                @endphp
                                <span class="px-3 py-1 inline-flex text-xs leading-5 font-semibold rounded-full bg-{{ $color }}-100 text-{{ $color }}-800">
                                    {{ $label }}
                                </span>
                            </td>
                            <td class="px-6 py-4 text-sm text-gray-500">
                                {{ $project->created_at->diffForHumans() }}
                            </td>
                            <td class="px-6 py-4 text-sm font-medium">
                                <div class="flex gap-2">
                                    <a href="{{ route('najm-bahar.projects.show', $project) }}" class="text-blue-600 hover:text-blue-900">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    @if(in_array($project->status, ['draft', 'rejected']))
                                        <a href="{{ route('najm-bahar.projects.edit', $project) }}" class="text-amber-600 hover:text-amber-900">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                    @endif
                                    @if($project->status === 'draft')
                                        <form action="{{ route('najm-bahar.projects.submit', $project) }}" method="POST" class="inline">
                                            @csrf
                                            <button type="submit" class="text-green-600 hover:text-green-900" title="ارسال برای بررسی">
                                                <i class="fas fa-paper-plane"></i>
                                            </button>
                                        </form>
                                    @endif
                                </div>
                            </td>
                        </tr>
                    @endforeach
                </tbody>
            </table>
        @endif
    </div>
</div>
@endsection
