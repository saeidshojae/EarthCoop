name: Archived Final Regression Patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Final regression patch already applied; workflow archived."
