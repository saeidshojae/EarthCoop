name: Apply Integration Contract Repair

on:
  push:
    branches:
      - agent/integration-hoda-bahar-after-groups

permissions:
  contents: write

jobs:
  apply-repair:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout integration branch
        uses: actions/checkout@v4
        with:
          ref: agent/integration-hoda-bahar-after-groups
          fetch-depth: 0

      - name: Apply canonical contract repair
        run: python3 scripts/integration_contract_repair.py

      - name: Apply investment financial boundary repair
        run: python3 scripts/integration_financial_boundary_repair.py

      - name: Apply welcome view safety repair
        run: python3 scripts/integration_view_repair.py

      - name: Commit repaired integration contracts
        run: |
          git restore .github/workflows/integration-full-validation.yml
          if git diff --quiet -- app tests resources; then
            echo "No integration repair changes required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app tests resources
          git commit -m "Apply integration safety repairs"
          git pull --rebase origin agent/integration-hoda-bahar-after-groups
          git push origin HEAD:agent/integration-hoda-bahar-after-groups
