# Laravel 10 Full CI Trigger

This marker exists only to trigger the full Najm Hoda hardening CI after the Composer-generated Laravel 10 baseline commit.

It intentionally does not trigger the dependency resolver workflow.
