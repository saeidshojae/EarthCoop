# PhpSpreadsheet Resolver Trigger

This marker exists only to trigger the controlled dependency resolver on `agent/najm-hoda-hardening`.

Retry: prepare Laravel writable directories before Composer install.
