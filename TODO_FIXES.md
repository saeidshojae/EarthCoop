# لیست اصلاحات مورد نیاز - Group Chat MessageController

## 🔴 اصلاحات بحرانی (Critical)

### 1. پیاده‌سازی متدهای گم‌شده در کنترلر (اولویت بالا)
متدهای زیر در `routes/web.php` تعریف شده‌اند اما در `MessageController.php` وجود ندارند:
- [ ] `markAsRead` - علامت‌گذاری پیام به عنوان خوانده شده
- [ ] `updateLastReadMessage` - به‌روزرسانی آخرین پیام خوانده شده
- [ ] `typing` - نشانگر تایپ (typing indicator)
- [ ] `searchUsersForMention` - جستجوی کاربران برای منشن (@)
- [ ] `search` - جستجوی پیام‌ها در گروه

### 2. اصلاح ناسازگاری HTML/Text در ویرایش پیام
- [ ] در متد `edit` محتوا با `nl2br(e())` ذخیره می‌شود (تبدیل خط جدید به `<br>`)
- [ ] فرانت‌اند در `submitEdit` از `textContent` استفاده می‌کند که `<br>` را به صورت متن خام نمایش می‌دهد
- [ ] باید فرانت‌اند `innerHTML` استفاده کند یا بک‌اند متن ساده برگرداند

## 🟡 اصلاحات کیفیت (Quality Improvements)

### 3. کاهش لاگ‌های Verbose در متد store
- [ ] حذف یا غیرفعال‌سازی لاگ‌های `[STORE_TIMING]` در محیط production
- [ ] یا قرار دادن آن‌ها تحت یک feature flag

### 4. یکپارچه‌سازی پاسخ‌های Rate Limiting (429)
- [ ] خط 120-122: ساختار `{'message': '...'}`
- [ ] خط 146: ساختار آرایه ساده `['An error occurred...']`
- [ ] باید ساختار یکسان باشد تا فرانت‌اند بتواند به درستی پردازش کند

### 5. پاک‌سازی فایل‌های بک‌آپ
- [ ] حذف فایل‌های `.bak`، `.encbak`، `.pre_*.bak` از کنترلر و فایل‌های JS

## 🟢 نکات دیگر (Optional)
- [ ] متد `processMentions` در حال حاضر stub است (خط 347-357) - می‌تواند پیاده‌سازی شود